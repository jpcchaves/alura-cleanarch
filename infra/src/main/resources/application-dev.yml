spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/cleanarch_db
    username: postgres
    password: Jp@123456
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true
  web:
    locale-resolver: fixed
    locale: pt_BR